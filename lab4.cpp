#include<iostream>
using namespace std;

struct node
{
    int data;
    node *left;
    node *right;
};

int main()
{
    while(cin>>input)
    {
        Node *mode=new Node();
        node->data=input;
        node->left=NULL;
        node->right=NULL;

        if(root==NULL)
        {
            root=node;
        }
        else
        {
           if(input<root->data)
           {
               curr=root->left;
           }
           else

             curr=root->right;

           while(curr!=NULL)
           {

           }
        }
    }


    return 0;
}
